import streamlit as st
from groq import Groq
import random
import time

# --- 1. КИБЕР-ДИЗАЙН (CSS) ---
st.set_page_config(page_title="AIS MASTER ELITE", page_icon="🖥", layout="wide")

st.markdown("""
<style>
    .reportview-container { background: #0e1117; }
    .stTextArea textarea { background-color: #161b22; color: #e6edf3; border: 1px solid #30363d; }
    .stButton>button { width: 100%; border-radius: 5px; height: 3em; background-color: #238636; color: white; border: none; }
    .stButton>button:hover { background-color: #2ea043; border: 1px solid #00ff66; }
    
    /* Хакерский терминал */
    .terminal-box {
        background-color: #050505;
        border-left: 5px solid #00ff66;
        padding: 20px;
        border-radius: 5px;
        box-shadow: 0 0 20px rgba(0, 255, 102, 0.1);
        margin: 15px 0;
    }
    .terminal-text {
        color: #00ff66;
        font-family: 'Consolas', 'Monaco', monospace !important;
        font-size: 14px !important;
        text-shadow: 0 0 5px rgba(0, 255, 102, 0.5);
    }
</style>
""", unsafe_allow_html=True)

# --- 2. ИНИЦИАЛИЗАЦИЯ GROQ ---
if "GROQ_KEY" in st.secrets:
    client = Groq(api_key=st.secrets["GROQ_KEY"])
else:
    st.error("FATAL ERROR: GROQ_KEY NOT FOUND IN SECRETS")
    st.stop()

def call_ai(system_prompt, user_input):
    try:
        completion = client.chat.completions.create(
            model="llama-3.3-70b-versatile",
            messages=[
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_input}
            ],
            temperature=0.4, # Сделаем ответы более точными
        )
        return completion.choices[0].message.content
    except Exception as e:
        return f"CRITICAL SYSTEM ERROR: {e}"

def print_terminal(text):
    st.markdown(f'<div class="terminal-box"><p class="terminal-text">{text}</p></div>', unsafe_allow_html=True)


# --- БАЗА ДАННЫХ (25 БИЛЕТОВ) ---
tickets_data = {
    "1": {"q1": "Понятие технической поддержки АИС", "a1": "Комплекс услуг по обеспечению бесперебойной работы системы и помощи пользователям.", "q2": "Классификация АИС", "a2": "По сфере применения, масштабу и характеру задач.", "pract": "Определить тип АИС по описанию.", "p_sol": "Анализ масштаба и функционала."},
    "2": {"q1": "Назначение системного ПО", "a1": "Управление ресурсами компьютера и создание среды для работы прикладных программ.", "q2": "Требования к установке ОС", "a2": "Минимальные требования (ОЗУ, диск), совместимость и лицензия.", "pract": "Выполнить план установки ОС.", "p_sol": "Подготовка носителя -> BIOS -> Разметка -> Установка."},
    "3": {"q1": "Этапы установки ОС", "a1": "Подготовка, загрузка, разметка диска, копирование, настройка параметров.", "q2": "Типы загрузочных носителей", "a2": "USB, CD/DVD, Сеть (PXE), внешние HDD.", "pract": "Создать структуру загрузочной флешки.", "p_sol": "Rufus/Ventoy, выбор ISO и схемы GPT/MBR."},
    "4": {"q1": "Прикладное ПО: виды", "a1": "ПО для задач пользователя: общее, специальное, развлекательное.", "q2": "Критерии выбора ПО", "a2": "Функционал, цена, системные требования, безопасность.", "pract": "Выбрать ПО для отдела продаж.", "p_sol": "CRM + IP-телефония + Офисный пакет."},
    "5": {"q1": "Методы установки ПО", "a1": "Локальная, сетевая, портативная, тихая.", "q2": "Форматы пакетов", "a2": "EXE, MSI, DMG, DEB/RPM.", "pract": "Проверить запуск установленной программы.", "p_sol": "Запуск от админа -> Проверка функций."},
    "6": {"q1": "Обновление системного ПО", "a1": "Установка патчей безопасности и правок.", "q2": "Назначение драйверов", "a2": "Связь ОС с аппаратным обеспечением.", "pract": "Проверить обновления ОС.", "p_sol": "Центр обновлений -> Поиск -> Установка."},
    "7": {"q1": "Архитектура АИС", "a1": "Структура связей компонентов (клиент-сервер).", "q2": "Основные компоненты АИС", "a2": "Техническое, программное, информационное обеспечение и персонал.", "pract": "Составить схему АИС.", "p_sol": "Клиент -> Сеть -> Сервер приложений -> БД."},
    "8": {"q1": "Ошибки установки ПО", "a1": "Несовместимость, права доступа, нехватка места.", "q2": "Методы диагностики", "a2": "Журналы событий, логи установки, коды ошибок.", "pract": "Решить проблему нехватки прав.", "p_sol": "Запуск от имени администратора."},
    "9": {"q1": "Виды ошибок ПО", "a1": "Логические, системные, интерфейсные, сетевые.", "q2": "Методы восстановления ПО", "a2": "Repair, переустановка, откат системы.", "pract": "Восстановление программы.", "p_sol": "Панель управления -> Изменить -> Восстановить."},
    "10": {"q1": "Системное окружение", "a1": "Версия ОС, библиотеки, зависимости.", "q2": "Совместимость", "a2": "Способность ПО работать совместно.", "pract": "Проверить совместимость.", "p_sol": "Сравнение системы с требованиями программы."},
    "11": {"q1": "Резервное копирование", "a1": "Создание дубликатов данных для восстановления.", "q2": "Способы восстановления", "a2": "Из образа, из файла, через спец. софт.", "pract": "Настроить бэкап каталога.", "p_sol": "Выбор источника -> Назначение цели -> Расписание."},
    "12": {"q1": "Типы резервирования", "a1": "Полное, инкрементальное, дифференциальное.", "q2": "Средства бэкапа", "a2": "Встроенные (Windows), сторонние (Veeam, Acronis).", "pract": "План бэкапа на неделю.", "p_sol": "Full в выходные, Incremental ежедневно."},
    "13": {"q1": "Интеграция АИС", "a1": "Связывание систем для автоматического обмена данными.", "q2": "Методы интеграции", "a2": "Через файлы, через общую БД, через API.", "pract": "Пример интеграции.", "p_sol": "Интернет-магазин -> API -> 1С."},
    "14": {"q1": "Протоколы взаимодействия", "a1": "Правила передачи данных (TCP/IP, HTTP, FTP).", "q2": "Совместимость модулей", "a2": "Возможность стыковки разных программных блоков.", "pract": "Выбрать протокол.", "p_sol": "Веб — HTTPS, почта — SMTP."},
    "15": {"q1": "Мониторинг АИС", "a1": "Слежение за работоспособностью в реальном времени.", "q2": "Средства администрирования", "a2": "Диспетчер задач, консоли MMC, удаленный доступ.", "pract": "Мониторинг службы.", "p_sol": "Настройка перезапуска службы при сбое."},
    "16": {"q1": "Информ. безопасность", "a1": "Защита от несанкционированного доступа.", "q2": "Типы угроз", "a2": "Вредоносное ПО, фишинг, инсайдеры.", "pract": "Поиск уязвимостей.", "p_sol": "Сканирование портов и версий ПО."},
    "17": {"q1": "Service Desk", "a1": "Централизованная обработка обращений.", "q2": "SLA", "a2": "Соглашение об уровне сервиса (время отклика).", "pract": "Распределить заявки.", "p_sol": "Приоритет: Критичный -> Высокий -> Нормальный."},
    "18": {"q1": "Алгоритм техподдержки", "a1": "Регистрация -> Анализ -> Решение -> Закрытие.", "q2": "Категории заявок", "a2": "Инцидент, Запрос на обслуживание, Запрос на изменение.", "pract": "Классифицировать обращение.", "p_sol": "Определение типа проблемы."},
    "19": {"q1": "Установка обновлений АИС", "a1": "Внедрение новых версий ПО в рабочую среду.", "q2": "Требования к обновлениям", "a2": "Тестирование на копии, план отката.", "pract": "Регламент обновлений.", "p_sol": "Backup -> Update -> Test -> Accept/Rollback."},
    "20": {"q1": "Тестирование ПО", "a1": "Проверка соответствия программы требованиям.", "q2": "Цель тестирования", "a2": "Выявление багов после запуска.", "pract": "Выбрать тип теста.", "p_sol": "Регрессионное тестирование при обновлении."},
    "21": {"q1": "Совместимость со сторонним ПО", "a1": "Работа АИС с внешними утилитами.", "q2": "Интеграционные ошибки", "a2": "Конфликты форматов, таймауты.", "pract": "Анализ ошибки интеграции.", "p_sol": "Проверка логов обмена данными."},
    "22": {"q1": "Безопасность при сопровождении", "a1": "Защита системы в процессе её эксплуатации.", "q2": "Политики безопасности", "a2": "Набор правил доступа (пароли, права).", "pract": "Настроить права доступа.", "p_sol": "Минимальные привилегии для папки."},
    "23": {"q1": "Администрирование АИС", "a1": "Управление пользователями и конфигурацией.", "q2": "Мониторинг нагрузки", "a2": "Контроль CPU, RAM и диска.", "pract": "Анализ загрузки.", "p_sol": "Поиск процесса через системный монитор."},
    "24": {"q1": "Документация по сопровождению", "a1": "Руководства админа, инструкции пользователя.", "q2": "Требования к документации", "a2": "Актуальность, полнота, структура.", "pract": "Создать инструкцию.", "p_sol": "Шаблон: Скриншот -> Описание -> Результат."},
    "25": {"q1": "Ошибки пользователей", "a1": "Некорректный ввод, удаление файлов.", "q2": "Методика анализа", "a2": "Сбор статистики обращений для выявления проблем.", "pract": "Разбор обращений.", "p_sol": "Поиск общей причины жалоб."}
}

# --- 4. УПРАВЛЕНИЕ СОСТОЯНИЕМ (SESSION STATE) ---
if 'page' not in st.session_state: st.session_state.page = "main"
if 'ex_ticket' not in st.session_state: st.session_state.ex_ticket = None
if 'ex_step' not in st.session_state: st.session_state.ex_step = 1
if 'quiz_active' not in st.session_state: st.session_state.quiz_active = False
if 'quiz_count' not in st.session_state: st.session_state.quiz_count = 1
if 'quiz_topic' not in st.session_state: st.session_state.quiz_topic = ""

# --- 5. ОСНОВНОЙ ИНТЕРФЕЙС ---
st.title("🖥 AIS EXAM MASTER ELITE v3.0")
tabs = st.tabs(["📖 БАЗА ЗНАНИЙ", "🎫 ЭКЗАМЕН-СИМУЛЯТОР", "🧪 AI ЛАБОРАТОРИЯ", "🌍 УНИВЕРСАЛЬНЫЙ ТЕСТ"])

# --- ВКЛАДКА 1: УЧИТЬ ---
with tabs[0]:
    st.header("📚 Справочник специалиста")
    cols = st.columns(2)
    for i, num in enumerate(sorted(tickets_data.keys(), key=int)):
        with cols[i % 2].expander(f"📦 БИЛЕТ №{num}"):
            d = tickets_data[num]
            st.markdown(f"**Вопрос 1:** {d['q1']}")
            st.info(d['a1'])
            st.markdown(f"**Вопрос 2:** {d['q2']}")
            st.info(d['a2'])
            st.markdown("---")
            st.success(f"🛠 **ПРАКТИКА:** {d['pract']}")
            st.warning(f"📝 **АЛГОРИТМ:** {d['p_sol']}")

# --- ВКЛАДКА 2: ЭКЗАМЕН ---
with tabs[1]:
    st.header("🎫 Имитация государственного экзамена")
    
    if st.button("🎲 ВЫТЯНУТЬ СЛУЧАЙНЫЙ БИЛЕТ", use_container_width=True):
        st.session_state.ex_ticket = random.choice(list(tickets_data.keys()))
        st.session_state.ex_step = 1
        st.rerun()

    if st.session_state.ex_ticket:
        t_num = st.session_state.ex_ticket
        step = st.session_state.ex_step
        data = tickets_data[t_num]
        
        # Прогресс билета
        st.progress(step / 3)
        st.subheader(f"Билет №{t_num} | Шаг {step} из 3")
        
        q = data['q1'] if step == 1 else (data['q2'] if step == 2 else f"ПРАКТИЧЕСКАЯ ЗАДАЧА: {data['pract']}")
        ref = data['a1'] if step == 1 else (data['a2'] if step == 2 else data['p_sol'])
        
        st.info(q)
        user_ans = st.text_area("Введите технически обоснованный ответ:", key=f"ex_area_{step}_{t_num}", height=200)
        
        col_c, col_n = st.columns([1, 1])
        if col_c.button("📊 СДАТЬ НА АНАЛИЗ"):
            if user_ans:
                with st.spinner("СИСТЕМА ПРОВЕРЯЕТ ВАШУ КВАЛИФИКАЦИЮ..."):
                    sys_p = "Ты — Senior IT Architect. Твоя задача — провести жесткий аудит знаний студента. Ты не прощаешь ошибок. Если ответ слабый — ставь 2. Если хороший — укажи на профессиональные нюансы."
                    user_p = f"Вопрос: {q}\nЭталон: {ref}\nОтвет студента: {user_ans}\nФормат ответа: 1.Вердикт (Оценка), 2.Критика, 3.Как ответил бы профессионал."
                    res = call_ai(sys_p, user_p)
                    print_terminal(res)
        
        if step < 3:
            if col_n.button("СЛЕДУЮЩИЙ ВОПРОС 👉"):
                st.session_state.ex_step += 1
                st.rerun()
        else:
            if col_n.button("🏁 ЗАВЕРШИТЬ ЭКЗАМЕН"):
                st.balloons()
                st.session_state.ex_ticket = None
                st.rerun()

# --- ВКЛАДКА 3: AI ЛАБОРАТОРИЯ ---
with tabs[2]:
    st.header("🧪 Генератор инцидентов (AI LAB)")
    topic_l = st.selectbox("Выберите зону ответственности:", ["Кибербезопасность", "Сетевая инфраструктура", "Базы данных", "Отказоустойчивость"])
    diff_l = st.select_slider("Уровень сложности аварии:", ["Junior (Ошибка юзера)", "Middle (Сбой ПО)", "Senior (Катастрофа)"])
    
    if st.button("🔥 СГЕНЕРИРОВАТЬ ИНЦИДЕНТ"):
        with st.spinner("СИМУЛЯЦИЯ СБОЯ..."):
            p = f"Опиши серьезный ИТ-инцидент в крупной компании по теме {topic_l}. Уровень сложности: {diff_l}. Не пиши решение, только описание ситуации."
            st.session_state.lab_q = call_ai("Ты — диспетчер мониторинга АИС.", p)
    
    if 'lab_q' in st.session_state:
        st.error(st.session_state.lab_q)
        ans_lab = st.text_area("Ваш протокол устранения аварии:", height=200)
        if st.button("🔌 ПРОВЕРИТЬ ПРОТОКОЛ"):
            eval_p = f"Инцидент: {st.session_state.lab_q}\nРешение: {ans_lab}. Проверь по шагам. Если есть риск потери данных — ставь 0."
            print_terminal(call_ai("Ты — руководитель ИТ-департамента.", eval_p))

# --- ВКЛАДКА 4: ЛЮБАЯ ТЕМА (МАРАФОН) ---
with tabs[3]:
    st.header("🌍 Универсальный тест (10 уровней)")
    
    if not st.session_state.quiz_active:
        topic_input = st.text_input("Введи область знаний (хоть 'Квантовая физика', хоть 'Dota 2'):")
        if st.button("🚀 ЗАПУСТИТЬ МАРАФОН"):
            if topic_input:
                st.session_state.quiz_topic = topic_input
                st.session_state.quiz_count = 1
                st.session_state.quiz_active = True
                # Чистим старые вопросы
                for k in list(st.session_state.keys()):
                    if k.startswith("q_any_"): del st.session_state[k]
                st.rerun()
    else:
        # Интерфейс активного квиза
        q_num = st.session_state.quiz_count
        st.write(f"### Тема: {st.session_state.quiz_topic}")
        st.progress(q_num / 10)
        st.write(f"**Вопрос {q_num} из 10**")
        
        q_key = f"q_any_{q_num}"
        if q_key not in st.session_state:
            with st.spinner("ИИ ПРИДУМЫВАЕТ ВОПРОС..."):
                st.session_state[q_key] = call_ai("Ты ведущий интеллектуального шоу.", f"Задай сложный вопрос по теме {st.session_state.quiz_topic}. Номер вопроса {q_num}.")
        
        st.warning(st.session_state[q_key])
        user_quiz_ans = st.text_area("Твой ответ на вопрос:", key=f"ans_any_{q_num}", height=100)
        
        col_check, col_next = st.columns(2)
        
        if col_check.button("🧪 ПРОВЕРИТЬ"):
            if user_quiz_ans:
                with st.spinner("СУДЬЯ ДУМАЕТ..."):
                    eval_q = f"Вопрос: {st.session_state[q_key]}\nОтвет игрока: {user_quiz_ans}. Оцени строго, дай краткую справку."
                    print_terminal(call_ai("Ты эксперт в этой теме.", eval_q))
        
        if col_next.button("ДАЛЬШЕ ⏭"):
            if q_num < 10:
                st.session_state.quiz_count += 1
                st.rerun()
            else:
                st.balloons()
                st.success("МАРАФОН ОКОНЧЕН!")
                if st.button("ВЕРНУТЬСЯ К ВЫБОРУ ТЕМ"):
                    st.session_state.quiz_active = False
                    st.rerun()

    if st.session_state.quiz_active:
        if st.sidebar.button("❌ ПРЕРВАТЬ ТЕСТ"):
            st.session_state.quiz_active = False
            st.rerun()

